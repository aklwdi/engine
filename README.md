# engine
engine im working on  
very basic right now  
hope this gets alot of features
# build
use g++ main.cpp -lraylib -lGL -lm -lpthread -ldl -lrt -lX11  
also g++ engine.cpp -lraylib -lGL -lm -lpthread -ldl -lrt -lX11
# snippets
screenheight= : screen height  
screenwidth= : screen width  
use each line for 1 snippet  
snippets have to be the exact same for them to work
